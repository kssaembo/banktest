# 클래스뱅크 수정본 · 2026-09-07

## 먼저 할 일

1. 기존 운영 서비스는 그대로 두세요. 이 수정본은 **테스트 GitHub/Vercel**에 적용합니다.
2. Supabase SQL Editor의 프로젝트가 **hoktiaduvzoaeapymuqw**인지 주소에서 확인하세요.
3. `sql/05-learning-addons.sql` 전체를 한 번 실행합니다. 결과: `LEARNING_ADDONS_READY`.
4. `sql/06-mart-sales.sql` 전체를 한 번 실행합니다. 결과: `MART_SALES_READY`. 기존 상품 관리용 SQL(04)이 적용된 프로젝트에서 실행합니다.
5. 새 소스를 테스트 GitHub에 업로드하고 Vercel 재배포 후 테스트 브라우저 탭을 다시 여세요. 예전 버전의 테스트 탭은 닫아주세요.
6. 교사 대시보드 국고 카드 → **경제상식/경제뉴스 보상 설정**에서 금액을 설정하세요. 처음에는 모두 0입니다.
7. 가상 학생으로 아래 항목을 확인하세요. 확인 전 운영 프로젝트에 SQL을 실행하거나 실제 학생 데이터를 옮기지 마세요.

새 SQL은 기존 함수·테이블 구조·잔액을 수정하지 않고 부가기능용 테이블과 함수를 추가합니다. 실행만으로 화폐가 이동하지 않습니다. 재실행하면 이미 존재하는 객체에서 전체 트랜잭션이 취소됩니다. 01~04 초기화 SQL을 다시 실행하지 마세요.

## 구현 내용

- 그래프: 마우스 음영 제거, `value`를 예치액·평가 금액 등으로 변경, 원그래프 툴팁을 중앙 합계보다 위에 표시. 마우스 클릭의 사각 포커스 표시 제거. 키보드 접근용 포커스는 유지.
- 은행원 학생별 자산: 실제 숫자 축을 공유하는 3개 막대(전체 자산·현금·예금/주식). 0은 0 높이. 학생이 많으면 가로 스크롤. 마우스로 금액 확인, 클릭으로 자산 상세 확인.
- 레이아웃: 여러 겹으로 화면 높이를 확보하던 설정 제거. 바깥 화면만 스크롤하고, 내용이 적으면 화면 높이에 맞춤. 대시보드의 빈 거래 영역과 하단 여백 축소. 긴 내용과 팝업·긴 명단의 필요한 스크롤은 유지.
- 마트: 상품/서비스별 누적 판매량 막대. 결제 시 상품 수량과 결제를 같은 서버 작업으로 기록. 송금 알림 폭 확대, 계좌번호 입력 후 학생 정보 자동 조회.
- 자판: 띄어쓰기 점 제거, 넓은 공백 유지. 배지를 학생별로 분리하고 서버와 병합. 온라인 복귀 또는 다음 열기 때 미동기화분 재시도.
- 소리: 기본 버튼음 유지, 메뉴·실행·닫기 음색 구분. 중단된 오디오를 재개한 후 재생하도록 수정. 가이드 이동·공격 음 추가. 음소거 설정 유지.
- 보상 설정: 교사별 뉴스·경제상식 금액. 뉴스는 기사별 첫 이수 1회, 국고 출금과 학생 입금 및 거래 기록을 원자적으로 처리. 실제 지급 성공 때만 화폐 획득 애니메이션.

## 보상 규칙과 제한

- 보상은 국고에서 나옵니다. 화폐를 새로 발행하지 않습니다.
- 기사별 최초 완료 시 금액을 확정합니다. 같은 학생이 다시 의견을 제출하면 기존 의견을 수정하고, 다시 지급하지 않습니다.
- 국고 부족 시 의견·완료 기록을 보존하고 보상은 대기합니다. 국고 충전 후 의견을 다시 제출하면 최초에 확정한 금액을 지급합니다. 자동 일괄 지급은 없습니다.
- 이후 설정액을 바꿔도 지급 완료액과 대기 중 확정액은 바뀌지 않습니다. 0 설정은 이후 최초 완료에 적용됩니다.
- 예전 코드에서 이미 작성된 의견에는 소급 지급하지 않습니다. 예전의 1 지급 여부를 확실히 재구성할 수 없기 때문입니다.
- 거래 내역은 `경제뉴스: 기사 제목 이수 보상`으로 기록됩니다. 학생·기사 고유 기록과 서버 잠금으로 중복 지급을 방지합니다.
- 뉴스 AI 의견 검토는 기존 기능을 사용하므로 Gemini API 오류가 있으면 제출 전에 안내합니다. 서버는 공개 기사·학생 소속·입력 길이·보상 중복·국고 잔액을 확인합니다. AI 평가를 위조할 수 없는 서버 인증 방식까지 구현한 것은 아닙니다.
- **경제상식은 설정만 준비했습니다. 퀴즈 및 이수 판정이 없으므로 아직 화폐를 지급하지 않습니다.** 이미지 넘기기만으로 보상을 받는 통로는 만들지 않았습니다.
- SQL 미적용 시 보상 설정/제출과 서버 배지 동기화에 설치 안내가 나옵니다. 기본 금융 화면은 이 부가기능을 기다리지 않습니다. 배지는 기기에 저장됩니다.

## 배지와 판매량

- 기존 공용 배지는 자동 귀속시키지 않습니다. 학생 페이지에서 학생 선택 → 경제자판 → 배지도감 → **이 기기의 기존 배지 가져오기**. 해당 학생의 배지가 맞는지 확인한 뒤 1회 가져옵니다.
- 이후에는 같은 학생으로 접속하면 다른 기기에서도 배지를 확인할 수 있습니다. 기기 이름과 학생 ID를 혼동하지 않도록 실제로 같은 학생을 선택해 주세요.
- 배지 동기화는 수집 기록 저장 기능이며, 타이핑 실력을 서버에서 인증하는 시험 기능은 아닙니다. 화폐 보상과 연결하지 않았습니다.
- 판매량은 새 버전 적용 후 **등록 상품을 선택해 결제한 수량**만 집계합니다. 과거 결제·계산기에 직접 입력한 금액은 상품 수량을 알 수 없어 제외합니다. 마트 환불 송금은 특정 상품 취소 기록이 아니므로 누적 판매량을 차감하지 않습니다.
- 별도 SQL(06)이 없으면 상품 수량을 포함하는 새 결제는 실행 전 안내합니다. 기존 직접 금액 결제 방식은 유지됩니다.
- 기존 JSON 백업에는 새 부가기능의 서버 테이블이 자동 포함되지 않습니다. 복구 기능은 이전 요청대로 보류 상태입니다. DB를 별도로 내보낼 때 새 테이블도 포함해 보관해야 합니다.

## 안전성과 확인 범위

현재 테스트 프로젝트는 **등록된 테스터가 가상 학급 전체를 다루는 구조**입니다. 새 SQL도 이 접근 모델을 따릅니다. 실학생용 교사·학생별 권한 분리가 끝난 운영용 SQL이 아닙니다. 실제 DB 이전 전에는 학생 로그인과 서버 권한 연결, 학급별 접근 제한, 보상 검증 경로를 별도로 검토해야 합니다. 프론트엔드에서 선택한 학생 ID 자체가 신뢰할 수 있는 인증 수단은 아닙니다.

검증: TypeScript 검사, 기존 금융·인증 테스트 19개, 로컬 PostgreSQL(PGlite)에서 기존 함수 변경 없음·접근 거부·국고 합계 유지·재요청·설정액 변경·국고 부족 재시도·강제 오류 시 전체 취소·배지 병합·상품 결제/집계 검증. 로컬 Chrome에서 보상 설정 저장, 실제 오디오 컨텍스트 재생, 공통 3:5 축, 툴팁 겹침/클릭 표시, 반응형 화면을 확인했습니다.

PGlite는 연결을 직렬 처리하므로 실제 여러 DB 세션이 동시에 경합하는 부하 시험은 아닙니다. 서버 잠금과 고유 키를 적용했지만, 아래 두 기기 확인은 테스트 배포에서 한 번 더 해주세요. 운영 서버에는 연결하거나 배포하지 않았습니다.

### 테스트 배포 확인

- 뉴스 보상 7 설정 → 가상 학생 첫 의견 제출 → 학생 +7 / 국고 -7 / 거래 기록 각 1개.
- 두 브라우저에서 같은 기사 재제출 → 추가 보상 0. 설정액을 바꿔도 동일.
- 국고 부족 → 의견 보존·보상 대기 → 충전 후 재제출 → 최초 설정액 1회.
- 학생 A 배지 획득 → 다른 기기 A에게 표시 / 학생 B에게 표시되지 않음.
- 상품 2개 결제 → 결제 금액과 판매량 2개 증가. 실패한 결제에는 증가 없음.
- 학생·교사·은행원·마트 메뉴, 주식 확인, 가이드 이동·공격, 음소거 확인.
- PC와 태블릿에서 짧은 화면은 불필요한 세로 스크롤이 없고, 긴 화면은 아래 내용에 접근 가능.

## 경제상식: 다음 구현 방향

별도 채팅에서 이미지를 만들어 오실 필요는 없습니다. 현재 이미지 원고를 바탕으로 핵심 내용을 절반가량으로 줄여 **3장 카드와 퀴즈 초안**까지 제가 구성할 수 있습니다. 그림 속 글씨를 다시 이미지로 굳히기보다, 그림은 이미지로 두고 제목·설명은 화면 텍스트로 표시하는 편이 글씨 크기 조절·수정·모바일 읽기에 유리합니다. 원하시면 완성 카드 이미지 3장을 제공하시는 방식도 지원할 수 있습니다.

추천 진행 순서:

1. ‘돈의 역사’ 한 챕터를 먼저 시범 구성. 핵심 학습 목표 2~3개만 남기고 반복 설명·긴 일화는 ‘더 알아보기’로 이동.
2. 카드 1: 물물교환의 불편함. 카드 2: 돈의 변화와 교환의 약속. 카드 3: 은행과 신용을 생활에 연결. 카드당 핵심 그림 1개, 짧은 설명 2~3개.
3. 교사가 내용·난이도·사실관계를 검토한 뒤 나머지 챕터에 적용.
4. 마지막에 3문항: 개념 확인 1개, 생활 상황 선택 1개, 이유 연결 1개. 틀리면 해설을 보고 재도전. 정답 기준이 있는 퀴즈부터 시작하고 한 줄 의견은 선택 활동으로 둡니다. AI 없이 가능합니다.
5. 퀴즈 답은 서버에서 판정. 최종 통과한 첫 이수에만 보상. 다시 학습·재도전 가능. 챕터 제목이 수정돼도 같은 고정 ID를 사용하여 중복 보상을 막습니다.

예상 난이도는 중간입니다. 카드 화면은 낮음~중간, 서버 정답 판정·이수 기록·보상 연결은 중간입니다. 새 챕터/문항/진도 테이블로 분리하면 기존 금융 기능과의 충돌 범위를 작게 유지할 수 있습니다. 아직 구현하지 않았습니다.

## 새 아이디어 평가 — 아직 구현하지 않음

| 기능 | 난이도 | 기존 코드 영향 | 교육적 가치와 추천 방식 |
|---|---|---|---|
| 경제 이야기방 | 중간 | 주제·댓글 테이블과 별도 화면으로 구성하면 낮음. 학급별 열람/작성 권한은 반드시 필요 | 학급 예산·소비 문제를 근거와 함께 토론. 교사는 주제 등록·마감·댓글 관리, 학생은 의견 작성. 처음에는 중첩 댓글·좋아요·화폐 보상 없이 시작하는 편이 단순합니다. |
| 경제 선택 이야기 | 중간~높음 | 시나리오·선택지·응답을 분리하면 금융 거래 코드 영향은 낮음. 교사 편집기와 진행 저장이 작업량을 늘림 | 제한된 돈·시간에서 선택하고 포기한 대안을 설명하므로 기회비용 학습에 적합. ‘좋은 개념 대 나쁜 개념’ 대결보다 같은 상황에서 서로 다른 선택을 비교하도록 구성합니다. |

경제 선택 이야기는 월드컵 모양을 쓸 수 있지만 탈락한 선택이 무조건 나쁜 선택이라는 인상을 피하는 것이 좋습니다. 예: 같은 예산으로 지금 필요한 준비물을 살지, 원하는 물건을 위해 저축할지 선택 → 한 줄 이유 → 포기한 대안과 장단점 확인. 기본 시나리오를 불러오거나 교사가 편집하도록 구성할 수 있습니다. 처음에는 승패 점수보다 선택 이유와 전후 생각 변화를 보여주는 방식이 적합합니다.
