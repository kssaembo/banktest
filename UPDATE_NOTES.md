# 2026-09-06 테스트 업데이트

## 시각화·화면 복원 추가 개선

- 제공 이미지 13개를 프로젝트에 포함하고 역할 선택, 지갑, 자산, 뉴스, 완료·빈 화면에 적용
- 백그라운드 탭이 다시 만들어져도 사용자, 역할 화면, 세부 탭과 선택 학생을 즉시 복원
- 사용 안내 버튼을 세부 제목 옆으로 이동하고 버튼명을 `세부영역명 + 사용 안내`로 변경
- 기간을 지정하는 대시보드 전체 CSV와 은행원 학생 자산 CSV 추가
- 세금·펀드·기부·예금·주식·학생 자산에 상호작용 그래프와 상세 목록 추가
- 대시보드 순위 선택 그래프, 통화량·평균 자산 추이 팝업 추가
- 마트 품목 전체 배치, 사이드바 너비와 추가 버튼 너비 수정
- Gemini 크레딧 소진 오류를 이해하기 쉬운 안내로 변환

## 추가 기능 업데이트

- 교사 대시보드·직업·세금·펀드·기부·예금·마트에 현황 카드와 간단한 도식화 추가
- 교사·학생·은행·마트의 로그인과 마지막 화면을 복원하도록 저장 방식 개선
- 마트 품목관리와 계산대 자동 합계 기능 추가 (`database-test/04-add-mart-items.sql` 적용 필요)
- 핵심 화면에 공통 디자인의 `사용 안내` 팝업 추가
- 배지도감에서 미획득 배지도 실제 모습을 공개하고 획득 배지를 강조
- 학생 현황 CSV와 주요 학급 데이터 JSON 백업 추가
- 원본 경제뉴스의 추천 검토·승인, 주간 참여, 기사별·학생별 의견 화면 복원
- Gemini Edge Function을 안정 모델과 JSON 응답 방식으로 변경하고 실제 서버 오류 문구 표시
- Vite의 Gemini 비밀키 주입 코드 제거
- 경제상식 샘플 이미지 3개와 제작 규격 추가

- 화폐 발행 완료 안내를 서비스 디자인 팝업으로 변경
- 학생 QR 기준 주소를 `VITE_PUBLIC_APP_URL`로 고정할 수 있게 변경
- 직업 배정 학생 행의 테두리·선택 대비 강화
- 세금 날짜 입력에 `납부 마감일` 표시
- 은행원 학생 목록에 현재 잔액 표시
- 송금·예금·펀드·세금 등 작은 알림 팝업의 잘못된 전체 너비 수정
- 친구 계좌번호 예시를 `예)060101`로 변경
- 경제자판의 공백을 가운데점과 별도 배경으로 표시
- 경제뉴스를 모달 내부 기능으로 통합: 교사 직접 기사 등록·수정·삭제, 학생 열람·의견 등록·1톨 지급
- 모든 버튼에 기본 클릭 효과음과 음소거 버튼 추가
- 디자인 이미지와 추가 효과음 제작 안내: `DESIGN_ASSET_GUIDE.md`

## 새 Vercel 환경변수

`VITE_PUBLIC_APP_URL=https://banktest-taupe.vercel.app`

설정 후 새 배포가 필요합니다. 기존 Supabase 환경변수는 유지합니다.

## 경제뉴스 범위

직접 기사 등록과 학생 열람은 기존 `news_articles`, `news_comments` 테이블을 사용합니다.
실시간 뉴스 검색과 AI 추천·요약·검수는 함께 포함된 Supabase Edge Function을 새 프로젝트에 배포해야 합니다.
필요한 서버 비밀값은 `GEMINI_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`입니다.
이 값들은 Supabase Function Secrets에만 저장하며 GitHub·Vercel·소스코드에는 넣지 않습니다.

학생 QR의 주소 문제는 고쳤지만, 새 기기에서 QR을 스캔하면 테스트 환경 보호 로그인은 먼저 필요합니다.
이는 테스트 DB를 외부에 공개하지 않기 위한 현재 단계의 제한입니다. 운영용 QR 직접 로그인은 사용자별 인증 구조를 별도로 완성한 후 검증해야 합니다.
